# Python `tkinter`

Tkinter è un modulo Python per lo sviluppo di interfacce grafiche (GUI: Graphical User Interface). Il principale vantaggio di `tkinter` è che è presente di default in Python (sicuramente se installato con l’installer ufficiale da [python.org](https://www.python.org/)).